apiVersion: v1
kind: Service
metadata:
  name: catsanddogsgenerator-service
  namespace: dev
spec:
  selector:
    app: catsanddogsgenerator
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
